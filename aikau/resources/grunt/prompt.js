module.exports = function(grunt) {

   // Update the grunt config
   grunt.config.merge({
      prompt: {
         clientPatch: {
            options: {
               questions: []
            }
         },
      }
   });

};